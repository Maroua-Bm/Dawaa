import 'package:flutter/material.dart';

class trydetpharm extends StatelessWidget {
  const trydetpharm({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}